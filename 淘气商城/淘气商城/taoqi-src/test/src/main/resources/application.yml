server:
  port: 8081

spring:
  cloud:
    stream:
#      bindings:
#        input:
#          destination: sink-channel
#      default-binder: rabbit
      environment:
        spring:
          rabbitmq:
            host: 127.0.0.1
            port: 5672
            username: guest
            password: guest
      binders:
        testOutPut:
          destination: testRabbit
          # 消息类型
          content-type: application/json
          # 设置要绑定的消息服务的具体设置
          binder: test
        orderOutPut:
          destination: testRabbit
          # 消息类型
          content-type: application/json
          # 设置要绑定的消息服务的具体设置
          binder: test



